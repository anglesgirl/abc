{
  "name": "safe-ao3-browser",
  "version": "0.1.0",
  "description": "Minimal Electron prototype that opens AO3 and attempts to enable DNS-over-HTTPS (DoH).",
  "main": "main.js",
  "scripts": {
    "start": "electron ."
  },
  "devDependencies": {
    "electron": "^26.0.0"
  }
}
